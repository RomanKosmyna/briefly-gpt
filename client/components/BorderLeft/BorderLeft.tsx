import { type FC } from 'react';

import styles from "./BorderLeft.module.css";

const BorderLeft: FC = () => {
  return (
    <span className={styles.border}></span>
  );
};

export default BorderLeft;